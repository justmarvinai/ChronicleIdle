/**
 * The Chronicler's Path's shape (docs/design/QUESTS_MISSIONS.md §4). Changing either number
 * changes what a finished chronicle means, so the content validator holds the line at them.
 */
export const MISSION_CHAPTER_COUNT = 10;
export const MISSIONS_PER_CHAPTER = 12;
